def maximum(a,b,c):
	if a>b:
		if a>c:
			return a
		else:
			return c
	else:
		if b>c:
			return b
		else:
			return c
	
a,b,c = input().split()
a,b,c = int(a), int(b), int(c)
print("이 세 수 중에서 maximum은 ",maximum(a,b,c),"입니다.")
